summary: Oracle Linux unsupported kernel
description: |
    Install unsupported unbreakable kernel (uek) on an Oracle Linux machine and verify, that the conversion is inhibited.

tier: 0

tag+:
    - kernel
    - inhibit-if-oracle-system-uses-not-standard-kernel

adjust+:
    - enabled: false
      when: >
          distro != oraclelinux

/unsupported_kernel:
    tag+:
        - unsupported-kernel
    test: |
        pytest -svv -m unsupported_kernel
