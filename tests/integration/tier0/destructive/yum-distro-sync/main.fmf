summary: |
    yum distro-sync edge-cases

description: |
    Verify the 'yum distro-sync' command edge-cases when given packages are not in the enabled repositories.

link:
    - verifies: https://issues.redhat.com/browse/RHELC-150

/yum_distro_sync:
    test: pytest -svv -m test_yum_distro_sync


#TODO(danmyway) merge with single-yum-transaction
