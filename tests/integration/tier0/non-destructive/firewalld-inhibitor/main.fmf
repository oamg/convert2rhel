summary: |
    Running firewalld inhibitor
description: |
    Verify that the inhibitor is raised, when the firewalld service is running before
    and during the conversion.
    This only applies to Oracle Linux (>=) 8.8.

enabled: false
adjust+:
    - enabled: true
      when: distro == oracle-8
      because: The test case applies to Oracle Linux 8.8

/firewalld_inhibitor:
    tag+:
        - firewalld-inhibitor
    test: |
        pytest -svv -m test_firewalld_inhibitor
