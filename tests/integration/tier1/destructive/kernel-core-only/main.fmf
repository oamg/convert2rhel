summary: |
    Only kernel-core package installed

description: |
  This tests sets up the machine that it contains only the `kernel-core` package installed
  on the system. Test is only viable to run on RHEL-8 like systems.
