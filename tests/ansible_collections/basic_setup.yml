- hosts: all
  tasks:
    - name: Building rpms
      delegate_to: localhost
      shell: make rpms
      args:
        chdir: ../..

    - name: Copying rpms to docker remote
      delegate_to: localhost
      shell: "docker cp .rpms {{ inventory_hostname }}:/data/"
      args:
        chdir: ../..
      when:
        


