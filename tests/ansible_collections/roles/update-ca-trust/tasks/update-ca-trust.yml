---
- name: Download the certificate to /etc/pki/ca-trust/source/anchors/
  get_url:
    url: "{{ lookup('ansible.builtin.env', 'CACERT_URL') }}"
    dest: /etc/pki/ca-trust/source/anchors/RH-IT-Root-CA.crt

- name: Update trust
  shell: update-ca-trust
  no_log: true
